---
interface Props {
  headline: string;
  ctaLabel?: string;
  ctaUrl?: string;
  background?: 'light' | 'dark';
}

const { headline, ctaLabel, ctaUrl, background = 'dark' } = Astro.props;
const isDark = background === 'dark';
---

<section class={`cta-strip ${isDark ? 'topo-bg' : 'topo-bg-light'}`}>
  <div class="container">
    <div class="cta-strip-inner">
      <h2 class={`cta-headline ${isDark ? '' : 'cta-headline--on-light'}`}>{headline}</h2>
      {ctaLabel && ctaUrl && (
        <a href={ctaUrl} class="btn btn-primary btn-lg">
          {ctaLabel}
        </a>
      )}
    </div>
  </div>
</section>

<style>
  .cta-strip {
    padding: clamp(3rem, 6vw, 5rem) var(--container-padding);
  }

  .cta-strip-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    text-align: center;
  }

  @media (min-width: 700px) {
    .cta-strip-inner {
      flex-direction: row;
      justify-content: space-between;
      text-align: left;
    }
  }

  .cta-headline {
    font-family: var(--font-heading);
    font-size: clamp(1.5rem, 3vw, 2.25rem);
    font-weight: var(--fw-heading);
    color: var(--text-on-dark);
    max-width: 24ch;
  }

  .cta-headline--on-light {
    color: var(--text-on-light);
  }
</style>
