---
import Nav from './Nav.astro';
import Footer from './Footer.astro';
import TinaIsland from '@tinacms/astro/TinaIsland.astro';
import '../../styles/global.css';
import { getSiteSettings } from '../../lib/data';
import { islands } from '../../lib/islands';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  schemas?: object[];
}

const settings = await getSiteSettings();
const site = settings?.data?.siteSettings;
const {
  title = site?.defaultSeoTitle ?? 'Contour',
  description = site?.defaultSeoDescription ?? '',
  ogImage = site?.defaultOgImage ?? '',
  schemas = [],
} = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    {ogImage && <meta property="og:image" content={ogImage} />}
    <meta property="og:type" content="website" />
    <title>{title}</title>
    {schemas.map((schema) => (
      <script type="application/ld+json" set:html={JSON.stringify(schema)} />
    ))}
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,400;0,9..144,600;0,9..144,700;1,9..144,400&family=DM+Sans:wght@300;400;500;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <a href="#main-content" class="skip-link">Skip to main content</a>
    <TinaIsland name="nav" wrapper={islands.nav.wrapper}>
      <Nav settings={site} />
    </TinaIsland>
    <main id="main-content">
      <slot />
    </main>
    <TinaIsland name="footer" wrapper={islands.footer.wrapper}>
      <Footer settings={site} />
    </TinaIsland>
  </body>
</html>
