A restrained section divider — a 1px gradient that fades transparent → teal → transparent, with an 8px teal dot at its center. Use only between major sections, not within content.

```jsx
<Divider />
```
