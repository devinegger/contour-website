node_modules/
dist/
.astro/
.wrangler/
tina/__generated__/.cache/
# Regenerated on every build; bakes the TinaCloud token from env when present.
# Never commit it (the repo is public). Build scripts recreate it before astro runs.
tina/__generated__/client.ts
.env
.env.local
.env.*.local
*.log

# Claude Design handoff archives
*-handoff.zip
test-results/
playwright-report/

# Local Claude Code config
.claude/

# Pricing reference PDF + its generator (local-only, not part of the site)
Contour-Pricing-Reference.pdf
scripts/build_pricing_pdf.py
